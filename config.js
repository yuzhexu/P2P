// Use this object as an export object for the current module.
module.exports={
	    // Define an attribute with a key name of port and a key value of 3000, 
        //indicating that the port number of the chat room server is 3000.

    "port":3000,
    // Define an attribute with the key name host and the value "127.0.0.1" (local IP address).
    //Indicates the hostname or IP address of the chat room server.
    "host":"127.0.0.1"
}
